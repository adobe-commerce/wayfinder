# Commerce DX Skills

## Validation

If using an external claude (`alias claude-ext 'CLAUDE_CONFIG_DIR=~/.claude-external claude`), make sure to allow WebFetch and WebSearch permissions otherwise it will only respond with training data.
